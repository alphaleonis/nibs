// Code generated by internal/membershipcontract. DO NOT EDIT.
// Regenerate with `task codegen`.
//
// The Go↔TS parity contract for the milestone-membership rules: a fixture of
// nibs as the wire reports them, each paired with the milestone id Go's
// membership.ResolvedMilestoneID (DIRECT assignment) and
// (*membership.View).MilestoneOf (DERIVED membership, inherited up the parent
// chain) resolved for it. membership.test.ts replays the fixture through the
// TypeScript mirrors and requires the same answers.
//
// `type` is the EFFECTIVE type, which is what the `Nib.type` resolver reports
// for a stored nib omitting the field; `parentId` is the RESOLVED parent, as
// the `Nib.parentId` resolver reports it; `milestone` is verbatim, as the wire
// gives it. So this is the client's view of the store the Go answers were
// computed over, not the store itself.
//
// The two answer columns were computed over two different lookups, and the
// replay has to match each: `resolvedMilestoneId` over a CANONICALIZING one
// (the `aliases` below), `milestoneOf` over exact ids, because it is a method
// on a View and a View indexes its slice by id alone. t6 is where the two
// part.
//
// The clause numbers in `note` are the direct rule's own order: 1 the subject
// is not itself a milestone, 2 the target exists, 3 the target is
// milestone-typed.

/** One fixture nib and the answers Go gave for it. */
export interface ContractCase {
  readonly id: string;
  readonly type: string;
  readonly milestone: string;
  /** The resolved parent: null for a root AND for a link naming no nib. */
  readonly parentId: string | null;
  /**
   * Other ids the lookup Go answered over resolves to this row, so the replay
   * can rebuild that lookup. A store lookup canonicalizes (nibcore Core.Get
   * tries the id, then the configured prefix prepended); a lookup built from
   * loaded rows on the client does not, so on the client this is always empty
   * in practice. The contract carries it because the Go rule is shared with
   * server-side callers whose lookup does canonicalize. It applies to
   * `resolvedMilestoneId` only — see the header.
   */
  readonly aliases: readonly string[];
  /** membership.ResolvedMilestoneID's answer, computed by Go at generation. */
  readonly resolvedMilestoneId: string;
  /** (*membership.View).MilestoneOf's answer, computed by Go at generation. */
  readonly milestoneOf: string;
  /** Which decision of which rule this row exercises. */
  readonly note: string;
}

export const MEMBERSHIP_CONTRACT: readonly ContractCase[] = [
  {
    id: "m1",
    type: "milestone",
    milestone: "",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "a milestone: a container of its own, never a member",
  },
  {
    id: "m2",
    type: "milestone",
    milestone: "m1",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "clause 1: a milestone-typed subject, even carrying a resolvable assignment",
  },
  {
    id: "m3",
    type: "milestone",
    milestone: "m3",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "clause 1: a milestone assigned to itself",
  },
  {
    id: "e1",
    type: "epic",
    milestone: "m1",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "m1",
    milestoneOf: "m1",
    note: "an ordinary resolving assignment",
  },
  {
    id: "t1",
    type: "task",
    milestone: "m1",
    parentId: "e1",
    aliases: [],
    resolvedMilestoneId: "m1",
    milestoneOf: "m1",
    note: "wire shape: a resolving assignment on a nib that also has a parent",
  },
  {
    id: "t2",
    type: "task",
    milestone: "",
    parentId: "e1",
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "m1",
    note: "the two rules part here: no assignment of its own, so the direct rule answers \"\" while the walk inherits its parent e1's m1",
  },
  {
    id: "t10",
    type: "task",
    milestone: "",
    parentId: "t2",
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "m1",
    note: "the walk is TRANSITIVE: neither t10 nor its parent t2 is assigned, so the answer comes from e1 two levels up",
  },
  {
    id: "t3",
    type: "task",
    milestone: "ghost",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "clause 2: the assignment names no nib in the fixture",
  },
  {
    id: "t4",
    type: "task",
    milestone: "e1",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "clause 3: the target exists but is not milestone-typed",
  },
  {
    id: "t5",
    type: "task",
    milestone: "m1",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "m1",
    milestoneOf: "m1",
    note: "wire shape: a stored nib omitting `type:`, which the wire reports as the default",
  },
  {
    id: "nibs-m4",
    type: "milestone",
    milestone: "",
    parentId: null,
    aliases: ["m4"],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "a milestone the lookup also answers for the short id \"m4\"",
  },
  {
    id: "t6",
    type: "task",
    milestone: "m4",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "nibs-m4",
    milestoneOf: "",
    note: "the answer is the TARGET's id, not the stored string: `milestone: m4` resolves to nibs-m4 for the direct rule, and for the walk only if the lookup canonicalizes",
  },
  {
    id: "m6",
    type: "milestone",
    milestone: "",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "a second milestone, so a nib and its parent can be assigned to different ones",
  },
  {
    id: "t7",
    type: "task",
    milestone: "m6",
    parentId: "e1",
    aliases: [],
    resolvedMilestoneId: "m6",
    milestoneOf: "m6",
    note: "the walk stops at the FIRST resolved assignment: its own m6, never its parent e1's m1",
  },
  {
    id: "m7",
    type: "milestone",
    milestone: "",
    parentId: "e1",
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "a milestone nested under an assigned epic: hand-edited decomposition data, and still a container of its own",
  },
  {
    id: "t8",
    type: "task",
    milestone: "",
    parentId: "m7",
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "the walk stops at the milestone-typed ancestor m7 rather than climbing on to e1's m1",
  },
  {
    id: "c1",
    type: "task",
    milestone: "",
    parentId: "c2",
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "a parent cycle with no assignment anywhere in it: the walk terminates on its visited set",
  },
  {
    id: "c2",
    type: "task",
    milestone: "",
    parentId: "c1",
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "the other half of the parent cycle",
  },
  {
    id: "t9",
    type: "task",
    milestone: "",
    parentId: null,
    aliases: [],
    resolvedMilestoneId: "",
    milestoneOf: "",
    note: "wire shape: a parent link naming no nib, which the wire reports as no parent at all",
  },
];
