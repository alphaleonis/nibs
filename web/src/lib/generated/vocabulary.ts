// Code generated by internal/webvocab. DO NOT EDIT.
// Regenerate with `task codegen`.
//
// The compiled-in vocabulary, rendered from the Go definitions
// (internal/config, internal/nibtypes, internal/estimate). The committed
// file is pinned byte-for-byte by TestGeneratedVocabularyIsFresh.

/** A status's lifecycle role — the Go config.Role enum. */
export type StatusRole = "open" | "startable" | "parked" | "done" | "dropped";

/** Statuses in rank order (Go config.DefaultStatuses) — the sort priority lists use. */
export const STATUS_DEFS: readonly { readonly name: string; readonly role: StatusRole }[] = [
  { name: "in-progress", role: "open" },
  { name: "todo", role: "startable" },
  { name: "draft", role: "open" },
  { name: "deferred", role: "parked" },
  { name: "completed", role: "done" },
  { name: "scrapped", role: "dropped" },
];

/** Statuses in workflow (transition) order — the order choosers list. */
export const STATUS_WORKFLOW_ORDER: readonly string[] = ["draft", "todo", "in-progress", "completed", "deferred", "scrapped"];

/** Status-group names shared with the CLI: `-s open` is `status:open`. */
export const STATUS_GROUP_OPEN = "open";
export const STATUS_GROUP_CLOSED = "closed";

export const TYPES: readonly string[] = ["milestone", "epic", "bug", "feature", "task", "research"];

/** Parent type → legal child types (Go nibtypes.ValidChildTypes). */
export const VALID_CHILD_TYPES: Record<string, readonly string[]> = {
  milestone: ["epic", "bug", "feature", "task", "research"],
  epic: ["bug", "feature", "task", "research"],
  bug: ["task", "research"],
  feature: ["task", "research"],
  task: [],
  research: [],
};

/** Container→leaf rank derived from the hierarchy: a leaf ranks 0, a container one above its highest child. */
export const TYPE_RANK: Record<string, number> = {
  milestone: 3,
  epic: 2,
  bug: 1,
  feature: 1,
  task: 0,
  research: 0,
};

export const PRIORITIES: readonly string[] = ["critical", "high", "normal", "low"];

/** Estimates with their point weights and display labels. */
export const ESTIMATE_DEFS: readonly { readonly name: string; readonly weight: number; readonly label: string }[] = [
  { name: "s", weight: 1, label: "Small" },
  { name: "m", weight: 3, label: "Medium" },
  { name: "l", weight: 5, label: "Large" },
  { name: "xl", weight: 8, label: "Extra Large" },
];

/** Creation defaults (Go config defaults for a new nib). */
export const DEFAULT_STATUS = "todo";
export const DEFAULT_TYPE = "task";
